locations = {

    ### Arcadia
    stadacona = { rank = city town_setup = empty_town }
    tadoussac = { rank = town town_setup = empty_town }
    matapedia = { rank = town town_setup = empty_town }

    ### Amazonie
    ananatuba = { rank = city town_setup = empty_town }
    kuruaya = { rank = town town_setup = empty_town }
    arua_amazon = { rank = town town_setup = empty_town }

    ### Jiyu
    puyallup = { rank = city town_setup = empty_town }
}